# Canonical generator quality acceptance

Status: active autonomous implementation and evaluation, September 4, 2026.

The owner has authorized continued implementation, fresh generations, evaluation, and coordinated deployment until the canonical generator consistently produces customer-ready local-business websites. Preserve the existing canonical architecture and one live authoring configuration. This is an engineering evaluation plan, not a new runtime phase, critic, retry mechanism, or product score.

## Current model constraint — September 7 owner decision

Astra must not be used for production authoring. Luna is the preferred production model; the next quality/cost comparison is Luna versus Terra. Sol remains an option only if a demonstrated quality need justifies evaluating it. Do not continue the previously proposed Astra high-versus-max experiment. Historical Astra runs remain retained diagnostic evidence, not proof of affordable production quality or an eligible production configuration.

The existing direct-OpenAI model-selection predicate now admits only Luna, Terra and Sol. Historical Astra and earlier-model pricing stays available for retained cost evidence, but does not grant new authoring eligibility. The catalog and settings validation use that same predicate, and manager execution rejects excluded models before a provider request. This is a constraint on existing selection, not a new routing or fallback layer; the production default remains Luna.

Close the demonstrated source-name and Google rating/link gaps, separating deployed code corrections from successful fresh-acquisition evidence. Independently compare Luna and Terra on identical, correct retained source, route plan, tools, guidance and reasoning effort, retaining every attempt; an acquisition-research permission issue need not block that isolated authoring comparison. Evaluate specific copy, imagery, responsive composition and factual defects alongside total model cost, duration and necessary edits. Do not change the live Luna default before evidence supports a decision. No additional agent stage, automatic critic or subjective hard gate is implied by this comparison.

The earlier experiment chronology below is retained; this current model constraint supersedes any prospective suggestion to select Astra. The separate browser-only research authorization question remains unresolved and is not implicitly overridden by permission to compare authoring models using retained evidence.

### Latest evidence — September 9

The corrected-source HD pair on release `5afcfbb3` is complete: Luna seven routes / $0.23949779 / 21.16 minutes; Terra five routes / $0.92296635 / 8.34 minutes. All 36 responsive route captures and shared menu states were reviewed. Both technical gates pass, but neither meets delivery acceptance. The captured Google rating/link and exact first-party quote bodies work; official identity, specific useful copy, contact hierarchy and business evidence still need improvement. Independent architecture/acquisition variation prevents causal model ranking. Keep Luna as the default and retain both rejected attempts.

The new diagnosis was an inconsistent logo contract, not another reason to increase model price. An opaque source crest reached the visual input without becoming a canonical asset, while the tool asserted it was already supplied and excluded logo adoption. The earlier Luna's icon workaround exposed that mismatch. The [source-logo amendment](decisions/2026-08-14-canonical-source-logo-authority.md) corrects the existing tool, preserving canonical preparation and owner-logo authority without a detector or new stage. The correction is deployed at `02f070bb`; coordinated release and independent identity/deep-health checks pass. Normal private Luna run `run_34fc38ae6c7a68e5de5fdf28717c0051` visibly uses the exact canonical crest without a special instruction, passes the hard gate, and records no transport timeout/replay/recycle. Its seven-route candidate costs $0.1636734 estimated and takes 17.24 minutes. All 21 route/viewport captures were reviewed; generic copy, phone contact hierarchy, a misleading form instruction, small-raster upscaling and source-copied technical inaccuracies still prevent delivery acceptance. This is one development observation, not a holdout or population reliability estimate. The earlier $0.0010173 forced-tool probe remains a separate mechanism test, not a full-site acceptance arm.

Next separate judgments: improve route-specific copy and action hierarchy through the existing authoring context/tools; complete the owner publication/edit journey; then reopen the fixed multi-business screen. Existing guidance already addresses the observed composition weaknesses, and the shorter-copy replacement probe found no material winner, so it is not adopted. A private reasoning-effort diagnostic is prepared but awaits explicit retained-payload export approval; no production setting changes follow from its preparation. Do not substitute another technical pass, hand-directed edit, or subjective numerical score for first-pass delivery acceptance. Detailed run IDs, failure chronology and release identities are in [current status](generated-site-authoring-status.md).

### Executing affordable-model screen

The first Luna/Terra pair uses release `4656e1276d33737c1da85c21cdd92189d722a33b`, the same current canonical authoring guidance, high reasoning, and a $5 ceiling per run. Both start blank through `bootstrapFromRetainedSite` and the ordinary hosted queue consumer, from private tree-service input `input_174f156002b94ddcac3a4f5fe7b76a30`. This input carries the corrected owner-approved disclosure documents and an existing browser-observed Google aggregate with its exact reviews URL. No new research, publication, inquiry, or notification is performed.

The existing workflow independently draws a Luna-high architecture in each arm. Do not add an execution selector or new production interface to force a shared plan for this screen. Record and compare those plans: this is an end-to-end configuration comparison with residual route-planning variation, not a controlled estimate of author-model effect alone. The stricter same-plan comparison above remains an ideal for attribution, not a reason to manufacture test-only production machinery.

- Luna: `run_0060d44f469a482299a42aca2572d1a8`.
- Terra: `run_534b42abdfed44479489dbb01ad75e04`.
- Full private experiment identities and evidence: the `affordable-comparison/` directory in the September 4 production-readiness evidence bundle.

Both initial attempts failed before model use with `owner_document_authority_invalid` (zero model requests or metered cost). The canary clone preserved the old site/source references inside the owner approval and reset its operational revision. The correction validates the original approval chain, rebinds only the copied authority references, recomputes the copied payload hash, preserves operational revision, and validates the derived input before persistence. No original approval, document, input, shared page, or artifact changes. The ordinary-workflow fixture verifies that the exact document reaches the cloned site's authoring context and that enqueue remains idempotent.

Corrected attempts are retained separately: Luna `run_bc84d080952d4337be56e0d18ddfa949`; Terra `run_84e7c8ae55f54b89aa7b912a153885bc`. Their operator bootstrap uses the locally verified correction, whose exact diff/hash is retained; execution still uses the unchanged deployed worker at the release above. The two pre-model failures remain in the experiment record and are not model-quality evidence.

Do not describe these as fresh acquisition tests or treat a gate pass as editorial acceptance. Retain every attempt, inspect all route types and actual responsive pixels, and distinguish optional polish from work required before customer delivery. Production remains Luna unless evidence supports a reviewed change.

### Initial-build output-detail treatment

The corrected Luna run finished with a passed hard gate, 33 routes, and $0.23187924 estimated model cost. Independent retained-artifact capture covered 99 route/viewport observations without load errors or horizontal overflow. Review still found overly compressed guide/service content, weak use of retained work imagery, internal evidence language, an unchanged hamburger in the opened menu, and an unsafe loss of the utility-first distinction in a generic power-line CTA. These are delivery findings, not new deterministic checks.

Before adding prompt rules, test the existing Responses output-detail setting: `medium` for initial authoring instead of `low`. Keep Luna, high reasoning, skill, tools, architecture, source, and verification unchanged. Edits, rebases and the structured architect request retain low detail. Record the effective value in each model request's existing telemetry. This is one parameter on the existing manager call, not a new profile selector, stage, retry, or critic. [OpenAI's deployment guidance](https://developers.openai.com/api/docs/guides/deployment-checklist#set-up-textverbosity) identifies medium detail as tending toward more structured coding output and notes that GPT-5.6 is already more concise. That supports a hypothesis, not a guarantee of better authored tool arguments or customer copy.

Accept only demonstrated gains in useful source-specific content, page composition, maintainable source, and review behavior without factual or functional regression; more words alone are not success. Compare the retained low-detail Luna/Terra attempts before interpreting the next Luna result. Keep all failed and successful attempts. Do not add a sentence for every reported aesthetic error or weaken the existing fact/authority boundary to make a sample pass.

### September 7 deployment interruption and reproduced sandbox races

CI `34166477173` passed for `2a6359d5`. Coordinated release `34166815573` then failed while setting Railway deployment variables: the GraphQL control-plane request timed out after approximately 90 seconds. Neither controller submission file was created, neither new Railway deployment appeared, and the sandbox pointer was not promoted. The prior web and worker deployments remained on `4656e127`; maintenance stayed active as designed. This is evidence of a deployment-control transport failure, not an application crash or a proven account-wide Railway outage. Preserve the failed release evidence; do not silently count a later attempt as a first-pass release.

The same release's first sandbox canary independently failed concurrent identical mutations with filesystem copy errors; its existing second full-canary attempt passed. That retry is not proof of correctness. A deterministic fixture executing the actual Worker lock function reproduced a fresh-lock race: exclusive `mkdir` succeeds, metadata publication pauses, and a concurrent request interprets absent metadata as stale and deletes the first request's lock. A second fixture reproduced a delayed status poll treating an already-completed, cleaned build process as failure. A third reproduced failure cleanup after a committed pointer rename whose RPC response was lost. These are infrastructure correctness defects, not evidence against the author model.

The correction removes lock stealing and poll-driven preparation restarts, re-reads queued journals after acquiring the mutation lock, and extends the existing finalization lock over process-status interpretation and terminal transitions. Promotion errors read back active ownership before any candidate cleanup, including when the rename's response is ambiguous; an unavailable read leaves the candidate intact. Truly abandoned preparation uses the existing client deadline and single fresh-sandbox recovery rather than a new in-place restart. No new lock identity, queue, service, model stage, retry counter, or customer-site presentation rule is added. The deployed synthetic canary now exercises ten independent concurrent-mutation pairs, requiring one mutation and retained replay for each pair, without retrying a failed pair.

The medium-output Luna generation remains unstarted until the corrected coordinated deployment is verified. Its authoring skill, prompt, source authority and model remain held; the infrastructure fix necessarily changes the exact sandbox/toolchain identity and must be recorded alongside the output-detail treatment rather than claimed as a completely unchanged environment. Existing retained inputs and artifacts are not rewritten.

## Editable draft correction — September 8

Coordinated release `34169432464` succeeded for `8663b07a`; web, worker and active sandbox report that exact release, deep health passed, and the maintenance lease was released. The deployed ten-pair mutation canary passed on its first attempt with an empty error log. Additional fresh-session verification and the medium-output Luna treatment remain separately retained; do not treat an ongoing screen as completed.

The medium-output treatment is `run_d6ab795d665a4cca8c066ad427aac33a`. Its actual request confirms Luna/high/medium. The first 73,884-character source write was rejected for two computed property accesses. Because the file was not saved, the model replaced the entire proposal: the next attempt fell to 55,720 characters and from 478 lines to 64, with a syntax error. This is a demonstrated tool affordance problem; it does not prove that every removed passage was useful or that a later tool correction alone guarantees better copy.

Remove the redundant per-mutation source-policy rejection from the manager's `write_file`, `apply_patch` and `edit_file` operations. Draft source is editable data and may temporarily be incomplete or invalid. Path/schema bounds, read-only source/authority boundaries, atomic multi-file updates and optimistic edit hashes remain. No draft is executed, inspected as a built site, or released merely because a write succeeds.

The existing `assertCompleteWorkspace` in the build operation still checks the entire source policy before the sandbox callback; the client and container independently retain their pre-execution checks. Inspection and finish both require this build, and every mutation invalidates prior build and verification caches. No allowed-language expansion, source-policy weakening, new model stage, repair continuation, retry, or permissive release fallback is introduced. Retained public artifacts and their original runtime bytes are unchanged.

Focused fixtures must prove that an invalid first draft is retained exactly, returns its actual editable hash, cannot execute or finish, cannot reuse a successful checkpoint, and survives resume so a one-line correction preserves the rest of the body. Forbidden imports must still fail before sandbox execution. Record the next fresh comparison against the unchanged medium-output treatment; the running treatment continues on its original deployed code and is not retroactively relabeled.

A read-only replay of the actual first proposal confirms that replacing only `sections[service.path]` and `policies[path]` with equivalent `Map.get` expressions clears all its source-policy findings, preserving the 73,884-character body (73,942 characters with the two expressions changed). This is evidence that whole-file regeneration was unnecessary, not a claim that the proposal was otherwise launch-ready. The focused manager fixtures, typecheck and authoring suite pass with the draft correction; full preflight and a fresh deployed generation still decide acceptance.

The current-authority audit exposed a separate context conflict: `ownerAuthority.approvedDocuments` correctly names the replacement privacy and cookie files, but all three paths in the generated source index still named the historical scraped files and their previews. The index now consumes the already-validated owner document references from the existing context. Source-sensitive document entries, route-to-file entries and detailed sources point to the replacement file only when its retained source snapshot and page IDs match; superseded previews are omitted. The original snapshot/page bytes remain available in the read-only mirror for history. No new authority store, approval, schema, or prose filter is introduced. The focused fixture reproduces the old missing-reference behavior and verifies all three corrected pointers plus rejection of a same-path/different-snapshot substitution.

The medium-output run completed with 33 routes, a passed hard gate and $0.29413902 estimated model cost. Independent retained-artifact review rejects it for delivery: generic service/article bodies, visibly crowded and duplicated footer links, unchanged opened-menu artwork, and internal review-provenance language remain. All 99 route/viewport captures loaded without page errors or horizontal overflow; Google rating/link and keyboard focus restoration are correct. Medium output detail alone did not establish a quality improvement. All ten additional fresh-session canaries passed, totaling 100 concurrent pairs beyond the release canary's ten. A local harness attempt without sandbox credentials failed before contacting a sandbox and is retained separately. No failed real canary was retried. The next release combines the two demonstrated authoring-boundary corrections; it is not an isolated causal estimate of either change's quality or cost effect. Focused regression checks and the complete local preflight passed before this change was committed; a fresh deployed generation still decides quality acceptance.

## Acceptance standard

### September 8 public-voice treatment and actionable source diagnostics

Release `a7314dc4` passed CI `34220092378` and coordinated production release `34220617965`. Both Railway services and the active sandbox reported the exact release SHA; deep health passed, maintenance was released, email remained disabled and the selected production model remained Luna. The first sandbox canary passed its ten concurrent mutation pairs. No public customer site was published.

Fresh Luna `run_cec6565e838d4ad68343af5f30c31a80` completed 33 routes with a passed hard gate, $0.30752043 estimated model cost and 1,611,014 ms recorded execution. Copy no longer routinely narrates internal source records, and several service bodies and the construction case study carry useful retained detail. It is still rejected for unattended delivery: Mike R.'s testimonial is paraphrased inside quotation marks, gallery captions/intro overstate what the selected photos demonstrate, and a small damaged-tree photo is enlarged. Phone hierarchy and some guide explanations also need improvement. Seven inspections repeatedly selected the same representative routes; a successful gate is not evidence of broad editorial review.

The exact-byte local capture attempted 99 route/viewport observations: 94 completed and five timed out waiting for retained images. Separate captures recovered additional views, including a visible phone close icon, Escape/focus restoration and scroll locking, but also encountered two further initial image waits. Preserve those attempts rather than claiming a clean capture. A bounded independent team-image download returned the expected 1,654,648 bytes and content hash in 11.8 seconds. The private layout harness now records hash-verified referenced-image download timing before browser navigation; this isolates layout review from broker transport and does not establish hosted image latency. Full evidence and delivery findings remain in the private `public-voice-luna/luna/DESIGN_REVIEW.md`.

The matched Terra run `run_f92e20e9a32b4567a434813c892cb633` is still running as this entry is written. Its early failures reveal a specific feedback defect: a local policy-data parameter named `document` receives only “Generated source uses forbidden browser runtime behavior.” The next attempt fixes a separate computed lookup but retains that identifier, fails again, and subsequently introduces a syntax error while renaming the policy component. This trace supports clearer diagnostics; it does not establish that all later repairs or final quality are caused by this one message.

Keep the existing conservative reserved-identifier restriction unchanged. The source checker now reports the exact identifier, line and column, explicitly including local bindings, and explains that local data can be renamed while the runtime capability remains unavailable. A fixture reproduces the harmless policy-object collision, verifies that renaming only the local identifier clears it, and verifies that actual `document`, `window`, `fetch`, `process` and `globalThis` references remain rejected. No scope resolver, new accepted language, orchestration, retry, or quality gate is added. The diagnostic text changes the fingerprinted toolchain provenance, not the source-policy contract identity; retained artifacts and runtime bytes remain untouched. Focused authoring checks, typecheck and the complete local preflight passed. Coordinated deployment and any fresh-generation effect remain separate pending evidence.

### September 8 affordable-model results and next context treatment

Release `03712416` completed through CI `34173266072` and coordinated release `34173597038`. Web, worker and active sandbox identities were independently checked, deep health passed, maintenance was released, email remained disabled and the live database-selected default remained Luna. The first release canary passed all ten concurrent mutation pairs without a retry. No retained authority or public artifact was rewritten.

Both fresh held-authority treatments finished with a passed hard gate and no observed sandbox replay, recycle or transport timeout:

- Luna `run_f71158ba24164a8eb5267f32025cf141`, version `version_be90e9a61393b2e7f34851fd798bc0d0`: 33 routes, $0.21854814 estimated model cost, 1,668,271 ms recorded execution. Review rejects internal research language, a false photo-upload promise, disproportionate headlines and lost case-study substance despite improvements in footer, service detail and form placement.
- Terra `run_118a2c73a5354b51a3351072289d5caf`, version `version_44c493a9ba2fa428ffde7abe53888938`: 36 routes, $2.02881856 estimated model cost, 2,075,946 ms recorded execution. Better composition, About content and case-study explanation; still too generic on important service pages, with weak educational prioritization and misdescribed gallery imagery. Two full-gate contrast failures were repaired. The second was a regression from darkening a shared accent without changing its button foreground.

Independent retained-artifact captures cover 99 Luna and 108 Terra route/viewport observations without recorded load errors or horizontal overflow. Both preserve the supported 4.7 Google rating and exact reviews destination. Both omitted authored expanded-state artwork while retaining working navigation state, labels, keyboard/focus behavior and scroll locking; do not describe that as another platform-icon collision. The private arm reviews distinguish delivery work from optional polish. Independent architect draws and n=1 per arm preclude a reliability or isolated model-effect claim. Neither result opens the unchanged eight-run acceptance screen.

Continue with Terra as the next editorial baseline, without changing the live default. The next treatment improves existing context and tool output, not production orchestration:

1. Clarify the single evidence boundary. Mapped first-party sources can support ordinary approved-service scope/process and documented project history, not only biography. Faithful educational explanations are allowed. Keep exact canonical support for sensitive commercial claims and all existing identity, capability, destination and owner-authority restrictions. A project's method or customer experience is not a universal commitment. This addresses a plausible contributor to generic copy; it is not yet a demonstrated causal diagnosis. Do not copy unapproved prices, credentials, guarantees or stale regulatory assertions to make a page longer.
2. Present inspection findings once, with errors first. Preserve visual and mechanical evidence, scoped routes, distinct examples, every image and raw diagnostic output. Remove only duplicate presentation, including a primary message repeated as its own example. A differing source, selector, message or affected-route set remains evidence. Do not remove the mechanical pass, add a critic, force a tool sequence, or change final verification.
3. Document the existing navigation CSS state hook beside the SDK contract: `toggleClassName` and `aria-expanded` belong to the rendered button, not the trigger child. Runtime still renders no presentation or fallback artwork; no recipe, component registry or fixed visual template is added.

A read-only replay of Terra's first retained inspection through the actual updated presentation boundary reduces its text from 48,767 to 28,347 bytes (41.9%), while preserving every distinct retained finding and all 40 image blocks byte-for-byte. This is an output-format result, not a model-cost saving or quality claim. Focused fixtures preserve mechanical errors, distinct advisories, images, scope and raw diagnostics. Fresh deployed generation remains the acceptance test; do not reclassify either old output as successful under the new context.

The new distinct-source fixture exposed an existing omission in the deduplication key. It now reuses the source/selector identity already used by root-cause grouping, so identical wording from different sources is not discarded before presentation. No new grouping mechanism or verification rule was introduced. The complete local preflight passed, and typecheck, authoring and focused manager tests were rerun after this correction. The next private Terra generation holds the retained authority, model, reasoning, output detail, toolchain and verification policy fixed; only the documented context/presentation treatment changes.

A customer-ready result needs no material corrective pass before delivery:

- Identity, contact details, geographic scope, services, credentials, quotations, and transactional claims agree with their retained authority. Source material is used accurately and in context.
- The homepage displays the exact supported Google aggregate rating and its supplied reviews URL together. No invented rating, URL, or individual third-party review prose. Browser research remains the authorized enrichment path; no paid Places API.
- The route set is proportionate to the business and its useful source material. Every retained route has a clear customer purpose, substantive relevant content, accurate metadata, and reachable navigation.
- Copy speaks directly to customers, uses concrete business evidence, and answers route-specific questions without internal research language or generic filler.
- Design is coherent across the site and suited to the business. Composition follows the content and customer action; shared shells are acceptable. Real imagery has appropriate resolution, role, caption, and crop.
- Desktop, tablet, phone, opened navigation, and form states work and look intentional. Objective safety, accessibility, factual, capability, and functional checks pass.
- A normal owner edit preserves unrelated source and behavior. Publishing and managed form delivery work through the released hosted system.

Subjective judgments remain advisory to product authoring. Review may reject an experiment for delivery quality without converting its taste findings into a hard release rule. Optional taste alternatives do not prevent acceptance, but missing evidence, weak primary copy, visibly unfinished composition, or a defective route does.

### September 8 public-copy delivery diagnostic

Release `409929f2` passed CI `34179437654` and coordinated release `34179774296` attempt 2. Attempt 1 failed on a Railway deployment-metadata read before maintenance, controller replacement or sandbox promotion; preserve that failure. Independent checks confirm the new web, worker and active sandbox identities, healthy authenticated deep health, inactive maintenance, disabled email and the unchanged Luna production default. Attempt 2's first sandbox canary passed all ten concurrent mutation pairs.

The private Terra treatment `run_08ed7b8d3f1941dab7d20212ee7e7417`, version `version_b7f40e892946534f103c57351392f7ca`, completed 32 routes with a first-pass hard gate, no failed authoring events and $1.580604 estimated model cost. Recorded execution was 1,054,027 ms; start-to-completion wall time was 18.17 minutes. Full service/article source reading and authored mobile close artwork both improved. Five inspections completed in 17.8–96.3 seconds, and full verification took 267.4 seconds. These are descriptive improvements over the prior 36-route Terra run, not a causal model or reliability result. A separate pre-enqueue operator connection failure was recovered only after exact-site readback proved no run had been created.

Delivery review still rejects public research narration, weak service scope explanations and a work gallery consisting of a team photo instead of available work examples. The author repaired the two routes named by a provenance advisory but left the same editorial problem elsewhere. Do not expand a keyword checker into semantic proof or add another production review stage. The completed source and all route captures are retained privately. The first local capture had three home navigation timeouts; the other 93 observations passed, and a separate unchanged-artifact home recapture passed all three sizes and menu states. This is complete eventual coverage, not first-attempt local capture success.

Two follow-ups remain distinct:

- An ordinary private Luna edit, `run_613ea3f9873647848c70d4e7adf0cc29`, produced version `version_765044e466a357dcf5573d657aed655e` at $0.16177036 estimated model cost and 12.05 minutes wall time. It repaired two authored TypeScript syntax failures, then passed its first full finish. It substantially removed research narration, restored supported removal scope and adopted three actual work photographs. Only five content declarations and gallery CSS changed; architecture, source index, destinations, shared shell, homepage, contact/form, reviews and legal declarations remained exact. All 96 route/viewport captures passed with no browser errors or overflow; selected native-pixel review confirmed the gallery improvement and retained menu behavior. Two 300px gallery photos are still stretched to 314–352px, and the construction guide remains thin with an unnecessary universal-result disclaimer. This demonstrates useful affordable editing, not initial-build acceptance, fully solved editorial quality or permission for an automatic critic.
- The next initial-build treatment changes only the existing prompt/skill boundary: the deliverable is the business's public website, with new copy in its customer-facing voice, not a report about retained evidence. Clarify that attribution applies to quotations and third-party statements, not internal source-record narration for the business's own supported descriptions. Preserve every factual, permission, source, route, runtime and verification constraint, and the owner's voice/edit scope. Move the existing voice instruction to the main role description instead of adding more repeated style rules. No new component, checker, workflow phase, automatic repair or model default is introduced.

This is a small hypothesis-driven prompt correction, not a proven fix. [Official GPT-5.6 prompting guidance](https://developers.openai.com/api/docs/guides/prompt-guidance-gpt-5p6) supports outcome-first instructions, concise tone definitions and surgical changes against actual traces; it does not establish the outcome of this experiment. Fresh deployed generation must validate the new brief before the unchanged eight-run screen opens. The frozen run's evidence is never relabeled under the new prompt.

The public-voice patch passed the full local preflight, including typecheck, build, authoring, browser rendering, trusted runtime, owner/inquiry fixtures and sandbox verification. The first restricted local attempt could not launch Chromium; the separately retained authorized browser run passed. No production runtime, verification policy, source policy or model default changes are bundled with this patch.

### September 8 smaller-site intake and visual-evidence correction

Release `190751a7` passed CI `34228463419`, coordinated release `34229066010`, and independent web/worker/Sandbox SHA and deep-health checks. Its first inactive-slot canary observed an old manifest before mutation testing; the next attempt passed all ten mutation pairs. Do not describe that as first-attempt canary success. Maintenance is released, notifications remain disabled, and the production author model remains Luna.

Fresh private HD Electric Luna run `run_cff1ea8ab8ab0119df1ef968245786cd` produced six routes and version `version_661b78b254a75072021a1ee7812eb9a9`, passed the hard gate at $0.17788571 estimated model cost and 16.69 minutes recorded execution, and had no recorded sandbox transport/replay failure. All 18 retained route/viewport captures completed without browser errors or horizontal overflow. Delivery review still rejects narrow repair-only positioning, conversation-heavy primary copy, poor mobile contact hierarchy and missing work imagery. Rating/link and exact first-party testimonial wording are positive findings. This is neither an unseen holdout nor a fixed-screen pass.

The retained evidence identifies specific upstream losses. A legacy services list occupies one heading with explicit line breaks and spaced columns; extraction discarded it and retained only Electrical Repair from a testimonial. The company story's explicit city list was lost by surrounding-area trimming and a filename-based evidence check. Work photographs were retained, but a CDN-hostname condition excluded them from photo-first visual selection while small homepage graphics filled the initial sheet. Explicit `data-bgimg` original URLs were also missed. These are context/discovery defects, not proof that a stronger author model is necessary.

Corrections extend the existing structural list parser and explicit gallery-URL discovery, preserve named narrative markets on first-party company-story pages, and remove the CDN restriction from advisory photo candidacy. They add no trade-specific service vocabulary, inferred image URL fetches, API enrichment, new extraction model, source schema, automatic adoption, critic or repair orchestration. Existing source provenance, URL safety, resource/run limits and sensitive authority boundaries remain. The source-backed service-scope guidance clarifies that positive offerings are not an exhaustive exclusion list; explicit owner restrictions still win.

The subjective `render.header_control_wrap` line-count advisory is removed, including its metrics and prompt-summary entries. It counted intentional stacked brand/CTA text as a defect and induced unnecessary flattening. Actual header collision, clipping, reachability, keyboard and focus checks remain unchanged. Taste stays in visual review rather than a new inference heuristic.

Two Luna-only architecture probes are retained. Wording alone failed to broaden the sparse input ($0.0044335, 34.2s). Re-extracting service/city facts from the exact retained HTML with the same prompt produced commercial/residential/light-industrial scope and the four named cities ($0.00332073, 30.5s). These are mechanism observations, not a stochastic comparison or acceptance of a generated website. Tests cover multiword service names and provenance, explicit original images, CDN candidates versus utility artwork, rejected blog/third-party geography, and retained functional header checks. The prior artifact remains unchanged. Coordinated deployment and a fresh Luna generation must validate the combined treatment before the unchanged fixed screen opens.

Private evidence and detailed review: `affordable-comparison/hd-luna/luna/` within the September 4 production-readiness evidence bundle.

## Sequence

1. Correct demonstrated verifier defects using retained artifacts and focused fixtures. The September 4 cookie-table diagnostic found 65.8% reported preservation versus 96.3% with proper text boundaries, without changing the website. Validate semantic text boundaries, table-header accessibility attributes, and reachable scrollable content. Keep content-deletion and real-clipping failures blocking.
2. Record a replay of the failed artifact under the corrected verifier before spending another generation on the same failure. Keep source and evaluation identities explicit.
3. Simplify the canonical authoring guidance and repeated inspection instructions around business truth, customer purpose, evidence, composition, and working behavior. Preserve useful tool contracts. Avoid adding a sentence for each historical aesthetic mistake.
4. Compare the existing authoring model and a stronger supported model using identical frozen business inputs, toolchain, guidance, reasoning setting, and evaluation. Retain all attempts, costs, durations, repairs, and final artifacts. Change one treatment variable at a time; do not infer stochastic superiority from one run.
5. Evaluate the selected configuration without further tuning on a declared six-business set covering a source-rich site, sparse source, visual trade, urgent service, geographic ambiguity, and a larger content estate. Include at least two businesses not used to tune that configuration. Repeat two cases independently. All eight final runs must meet the delivery standard; any material failure reopens the relevant defect and invalidates a claim that this set passed unchanged.
6. Complete the coordinated release, verify its exact identities and ordinary hosted authoring path, then exercise owner editing, publishing, and managed lead delivery. Record the remaining limitations before the final readiness decision.

The eight-run screen is a launch decision sample, not a population reliability estimate. Do not claim an established success percentage or quality superiority over R8 from close subjective totals. Review source fidelity and native route screenshots together; a contact sheet or gate pass alone is insufficient.

## Evidence and reporting

Retain run IDs, source/public-input hashes, model/skill/verification identities, first draft and final source where available, failures, model cost, total duration, repair behavior, and screenshots under `.design/`. Use the existing design review workflow for key routes at 1280×800, 768×1024, and 375×812, with whole-site route coverage and interactive states. Record must-fix, should-fix, and optional findings with their source or screenshot evidence.

Until the entire acceptance sequence is supported, report individual artifacts as candidates and the generator as under evaluation. Successful local tests, a deployed release, a good artifact, and a reliable customer workflow are separate claims.

## Concise-context treatment

The initial-build skill is reduced from 4,645 words to 1,519, its architecture handoff to 133 words, and repeated inspection guidance to approximately 200 words. The skill is the single location for substantive design and copy guidance; the handoff locates authority and evidence, while inspection supplies actual findings and their scope. Mechanical checks, returned findings, error precedence, model tools, deadlines, and final verification are unchanged. Edit and rebase instructions are unchanged. Tests retain authority and interface contracts rather than requiring a sentence for every historical aesthetic mistake.

The treatment hypothesis is improved attention to route evidence and customer purpose, not that fewer words are inherently better. It is unvalidated until fresh outputs pass review. The initial skill identity is `website-authoring@sha256:f682c27365ee62d70b6764bb153bc53ef89196f0d94d9385f11c31889a1cb167`. The platform descriptor is `site-authoring-platform:semantic-legal-and-scroll-verification:concise-authoring-contracts:2026-09-04`.

The first model comparison uses the same retained Good Morning Tree Service authority and source mirror for Luna and Sol, both at high reasoning with a $5 model-cost fuse. Acquisition is skipped deliberately; the existing hosted worker executes normal initial authoring. Architecture still uses the same canonical architect model independently in each arm, so resulting route-plan differences must be reported as residual variability, not attributed solely to authoring-model quality. This small screen determines which configuration merits further testing; it does not establish comparative reliability.
