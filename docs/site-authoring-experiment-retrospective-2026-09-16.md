# Fresh business source-authority diagnostic retrospective

## Result

The private Pristine Auto Detailing initial build on release `43f4273d79e0a418986b2623b96392cfc9626497` completed normally through the hosted queue. Run `run_37fd0e2e0e29497d7b4bede353a11b3c` produced `version_6d779530fe627e96b46aab6fd0dbc878`: 28 routes, 20m48.164s elapsed, $0.21134310 catalog-estimated model cost, OpenAI `gpt-5.6-luna`, passed hard gate. Two authored build failures were repaired within the normal run. They were a prohibited dynamic property lookup and an invalid top-level return, not evidence for relaxing the source policy. No transport failure is recorded in the collected events. No site was published, inquiry sent, or business contacted.

The prior candidate, Allied, was deferred after its public HTTPS origin failed TLS in the browser; no Allied generation was attempted. That source-readiness failure remains evidence, not a hidden successful sample. Pristine is a diagnostic, not a completed repeatability screen.

All 84 route/viewport captures (1280, 768, and 375 pixels) completed without recorded browser errors, broken visible images, or horizontal overflow. Actual pixel and all-route source review nevertheless rejects unassisted customer delivery. Technical success is not quality acceptance.

## Proven upstream defects and bounded corrections

- The homepage title selector chose the slogan “Austin's Premier Auto Spa” as the business name. The same homepage title also contains “Pristine Auto Detailing,” corroborated by the About and Contact names. A narrowly scoped tie-break now prefers an equally plausible homepage title segment independently selected on at least two other eligible first-party pages. Stronger existing names, ties and insufficient corroboration keep the existing selection. This is name disambiguation, not a general claim that repeated web prose proves truth.
- Broad non-exclusive page-purpose tags promoted fourteen portfolio project titles into service authority and excluded real service routes. Explicit service-route structure now takes precedence over incidental location tags; project/gallery routes remain retained evidence but do not become service names merely because their metadata mentions a service.
- Four specialist pages contain a stale Minnesota contact block. The actual Cedar Park address was missed because the source joins `Building 22A` to its city and `78613` to `USA`, and uses the previously unrecognized street suffix `Terrace`. The address grammar now recognizes these generic forms. Existing scoped-location selection, not a new geographic inference rule, decides among parsed addresses.
- Google research received that Minnesota location even though its address fact was explicitly not public-eligible. Research geography now follows the same eligible-fact authority as public projection. Rejected observations remain auditable but do not become trusted lookup hints. The old ambiguous Google result was appropriate given the conflicting request; no rating has been retroactively inserted.

Focused offline fixtures cover these cases and preserve owner-confirmed authority. Stored inputs, snapshots, workspaces and artifacts remain unchanged. The authoring platform descriptor changes to `site-authoring-platform@sha256:b78e501f2fa155a048d82cd67157f56777de643fa5d2e196f76e138bfdf730b7`; model, prompt, skill, runtime, toolchain and verification rules do not change. Deployment and a corrected fresh build must be recorded separately from local verification.

## Remaining delivery defects

Local verification is complete: full `npm run verify:preflight`, isolated `npm run smoke:dev`, focused ingestion/manager fixtures, TypeScript and whitespace checks passed. Independent review exposed two initial address-regex regressions (ordinary ZIP punctuation and ambiguous joined boundaries); both were fixed and rechecked before release. The final parser keeps ordinary delimiters and a separate conservative joined-building case. Full mocked ingestion proves the actual primary Cedar Park shape becomes public-eligible despite the conflicting specialist page. No broad claim of arbitrary malformed-address recovery follows.

The valid official logo is SVG, excluded by today's raster-only intake. No raw SVG adoption, permissive sanitizer, replacement mark, new dependency or rendering service was added in this correction. Safe SVG-to-raster intake remains a separate format-boundary task.

The author selected only two photos and reused the black Tesla hero across fourteen named vehicle-project routes. This misrepresents project imagery despite otherwise truthful photo alt text. Service bodies are too interchangeable; the long text-only mobile portfolio has excessive empty space. The About page's “Browse the portfolio” button incorrectly goes to Contact, and Auto Detailing was turned into a services hub with a self-link rather than retaining useful package depth. The original source also contains legal placeholders; faithful preservation of inherited defects is not launch-ready legal content.

The responsive shell, open mobile menu, explicit required/optional form guidance and inquiry—not instant-booking—semantics work. No individual review quotes or unsupported Google aggregate were invented. Those strengths do not cancel the defects.

## Next experiment boundary

First verify and release the source-preparation correction, then repeat a fresh private build with unchanged Luna authoring. Check the prepared identity, actual services, geography and Google observation before interpreting the visual result. Preserve this original failure. Do not treat a hand-repaired site as an unassisted pass, add an automatic reviewer, force tool sequences, or raise a subjective quality gate. If the corrected context still yields misleading imagery and shallow route bodies, compare Terra under the same evidence and review criteria rather than assuming more prompt text will solve the judgment issue.

Detailed retained evidence is under `.design/v4-production-readiness-2026-09-04/fresh-pristine-2026-09-16/` and exact-byte captures under the corresponding `candidate-reviews/version_6d779530fe627e96b46aab6fd0dbc878/fresh-pristine/` directory. Reports distinguish inspected pixels from automated coverage and local corrections from hosted deployment.
