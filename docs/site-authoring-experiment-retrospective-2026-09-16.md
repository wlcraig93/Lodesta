# Fresh business source-authority diagnostic retrospective

## September 17 local follow-up: assets and accurate sandbox errors

Three bounded corrections are implemented locally, not deployed and not evaluated in a new paid generation:

- Canonical source logos may use self-contained SVG inputs, rasterized by existing Sharp into PNG before the existing asset persistence path. General SVG photo/icon adoption remains forbidden. Exact source bytes/hash stay in the immutable source mirror; only PNG revisions/refs become public assets. The retained Pristine replay initially failed despite successful rasterization because filename normalization erased the `Logo` word boundary in `PristineTXNewLogo.svg`. Generic camel/acronym tokenization corrects this without business-specific selection. Independent review also caught percentage-only sizing producing a 1×1 asset; intrinsic dimensions are now required. The exact 12,832-byte retained input yields 331×77 PNG; all 101,948 decoded RGBA channels match a same-engine reference and the mark was visually reviewed. Conservative textual rejection and Sharp's cooperative timeout are not a general SVG sanitizer or hard process-memory isolation. Linux/native deployment validation remains outstanding.
- Google rating research records a top-level profile-URL disposition: accepted, not returned, or rejected. Offline mocked live-provider fixtures preserve aggregate authority and exact accepted URLs. A rejected structured URL is not copied into authority, while independent consulted-source evidence remains retained. No fabricated destination, paid Google endpoint, extra lookup or new research stage is added. This does not retroactively supply the missing candidate link or establish research reliability.
- After auditing the pinned SDK, journal reads now bypass its lossy `exists()` command. They call `readFile` once and interpret only structured `FILE_NOT_FOUND` as absence. All other errors propagate; malformed bytes still fail loudly. Existing extracted-function fixtures cover initial and under-lock read errors, no unintended writes, replay, exact error discrimination and the preserved preparing-owner boundary. No lock recovery, retry, timeout, state-machine or dependency change is introduced. The full local sandbox verification passes; provider sign-in is now resolved, with the distinct observed interruptions documented below, not a proven underlying cause or hosted fix.

Source treatment identity: `site-authoring-platform@sha256:6fa8e88563ac16f3801ec231a5cc38bd634570a6007a8391e7b67e0ab6b46c53`. Sandbox toolchain identity: `lodesta-static-site-workspace@sha256:29e672a0460ac4a4c3c770c3379e77902ad3f7843aeffe57ecb54d20e87464c0`. Prompt, skill, production model and hard verification policy remain unchanged. Local static, media and focused form/workflow checks pass. Exact logo replay evidence: `.design/v4-production-readiness-2026-09-04/svg-logo-validation-2026-09-17/`. SDK source and contract evidence: `release-5a7d426b/SDK_AUDIT.md` under the same readiness directory.

The earlier prepared paired-run scripts remain historical, unexecuted and pinned to the failed release. They must be re-prepared against the verified new release and a shared corrected source/asset input before execution; they must not silently run the old thirteen-offering input as a test of the eight-offering source correction. Hosted generation and deployment outcomes remain separate acceptance requirements.

Final local checks pass all preflight components (`verify:static`, `verify:browser`, `verify:sandbox`), TypeScript and `smoke:dev`; independent integration review found no remaining blocker. Restricted Chromium initially failed to launch and passed with approved process access. One overlapped smoke/typecheck attempt saw inconsistent generated `.next` route types; rebuilding and rerunning static verification sequentially passed without changing application source. These local checks do not establish Linux native-renderer fidelity, hosted sandbox reliability or fresh-site delivery quality. Nothing was committed, deployed or newly generated in this follow-up.

## Follow-up release failure, diagnosis and bounded release decision

Following the authenticated investigation and later unchanged-deployment canary, independent review supports a new exact corrective commit through the existing CI and coordinated release gates. The original failures remain unresolved as to provider trigger, not erased or declared fixed. No workflow or lifecycle workaround is introduced. The prior diagnostic hold is superseded only for this reviewed corrective release; paid comparisons still require exact released identities, a separate fresh synthetic canary and a newly prepared shared corrected input.

Follow-up commit `5a7d426be682e2d5aa57c90889f1f2f8867f7019` passed CI, but coordinated release `35166352222` failed on both inactive-green canaries before registration, maintenance or controller rollout. First apply retained a compiling journal, then recorded 337 HTTP errors with the last reporting explicit absence; the same build process later appeared completed. The second canary reached post-restore rebuilding, then retained a preparing journal and mutation lock without a visible matching process. That initial packet alone established neither a compilation defect, container restart, journal deletion nor provider root cause.

Actual-function offline reproductions distinguish ordinary rejection (terminal failure and lock release) from an unresolved preparing owner (visible nonterminal state and retained lock). They also exercise process-start/journal-write ambiguity and the explicit-absence status symptom. These tests pass together with TypeScript and add no runtime behavior, retry or timeout. Provider access was initially blocked on sign-in; that block is now resolved. The previous c354 controllers, active blue and maintenance-free state were verified unchanged at `2026-09-17T00:44:42Z`, with no deep-health errors.

Subsequent visible provider records establish that the first build completed before SIGTERM/exit 143 and an entrypoint restart, after roughly 13 seconds of active container life. The configured 15-minute idle duration therefore does not explain that termination. The second window instead shows simultaneous canceled RPCs and `Network connection lost`, with no observed shutdown/restart. Neither identifies the underlying trigger; the overlapping DO alarm is not causal proof. These are transcribed dashboard observations, not a complete raw export: `release-5a7d426b/PROVIDER_LOG_DIAGNOSIS.md` under the readiness evidence directory.

The fresh-session diagnostic subsequently passed at `2026-09-17T14:56:56Z` in 144.218 seconds on unchanged inactive green release `5a7d426b`, toolchain `76721e43…`. It passed ten concurrent mutation pairs, failed-build isolation, replay, backup/restore and final artifact checks. Independent review matched the receipt, archived canary/manifest source hashes and process exit 0, which includes awaited session cleanup. The earlier `f722d845` setup invocation lacked local configuration and made no hosted request. Retained evidence: `release-5a7d426b/inactive-green-diagnostics/2026-09-17T14-54-30-720Z-6fc6959e/`. No model call, deployment, registration or promotion occurred. This later pass neither erases the release failures nor proves their cause fixed, validates the undeployed `29e672a0…` correction or establishes promotion readiness. No paid comparison or release rerun follows automatically.

The subsequent exact-tag SDK audit narrows what “absence” means: Cloudflare Sandbox 0.12.7 `FileService.exists()` intentionally maps execution-service errors to a successful false response, including `SESSION_ERROR` in its official fixture. Consequently the first canary does not establish that a successful filesystem check found no journal. A masked execution failure is now a concrete candidate, not a proven provider root cause. Relevant file-service/RPC source is byte-identical in 0.12.8 and 0.12.9; those releases only fix R2 mount handling. Journal writes acknowledge direct `Bun.write` without atomic replacement or commit reconciliation, while outer HTTP cancellation is not demonstrably wired into each file RPC. Preserve those distinctions rather than declaring a lifecycle fix or upgrading speculatively. Exact upstream references and limitations: `release-5a7d426b/SDK_AUDIT.md` beneath the evidence directory.

The earlier model comparison is prepared but unexecuted and must not run unchanged. Both new Luna and Terra arms need the same corrected source authority and assets, prepared after the verified corrective release; comparing different inputs would confound the result. Fresh architecture selection is independently sampled, so even paired observations will not establish a causal model ranking or repeatability. Retained-input comparisons alone do not test fresh URL acquisition. Paid runs remain held until release and independent canary acceptance; the reviewed new release decision above supersedes the earlier blanket release hold. Full release evidence, active-state readback and reproduction limitations are retained under `.design/v4-production-readiness-2026-09-04/release-5a7d426b/`.

## Corrected-release comparison and follow-up

Release `c3545a8fff6c93f6f3c88f83b1657952b1b6ea59` passed coordinated release `35161701490` attempt 1, exact web/worker/sandbox identity checks and a separate fresh-session ten-pair synthetic canary. Maintenance was released and email remained disabled. The ordinary hosted queue then produced private Luna candidate `version_546aa5219e8c28d4794398b13fa43f48` from run `run_7b266e823f43d3dcac60a5b6f7c6785c`: 16 routes, 22m20.567s wall time, $0.29562455 catalog-estimated model cost, hard-gate pass. No publication or inquiry occurred.

The prepared name, Cedar Park address and matched Google 4.9 observation are corrected. Research supplied no profile URL; the author appropriately omitted an invented destination. All 89 page-text hashes match the original diagnostic. Five fallback service labels remain because every real service page contains isolated placeholder components and was downgraded by a page-wide keyword test. The local follow-up retains specifically titled ambiguous customer pages, while preserving title/CMS/admin exclusions and content-level proof checks. Exact full ingestion of all 89 retained documents returns exactly eight services and correct contact/location facts. Four stale specialist pages also become first-party origin-eligible; that is not factual endorsement. Their incorrect facts remain suppressed by existing primary-page/consensus/direct-offering selection in the replay and a focused end-to-end fixture. No path exception, score, second extractor or business-specific stale-address rule is added.

A second small defect selected empty normalized source blocks as contact/address citations because every string includes the empty string. A minimum meaningful-block length fixes that without changing fact values or retained artifacts. The platform descriptor is `site-authoring-platform@sha256:9b6984884e41ac81948b33e62bc62c91c30824a7c4f0ba87a9421377a0a1ff3b`; model, prompt, skill, runtime, toolchain and verification rules remain unchanged. Full local preflight and smoke passed, followed by an additional final-state ingestion fixture. These corrections are not part of the completed run and require a separately recorded deployment.

All 48 route/viewport captures of the new candidate completed without recorded errors or horizontal overflow. Pixel review still rejects delivery: severe mid-word heading wrapping, weak project evidence, repetitive service structure, an empty Hours heading and inherited legal placeholders. The opened navigation compresses into a 48px scrollport: authored absolute positioning competes with the modal bottom inset. A diagnostic fixed-position-only derivative restores its intended size. Independently scrolling each retained-panel link and clicking its visible intersection proves all six links remain reachable at phone and tablet widths. Thus it is a serious visual/discoverability defect, not proof that a functional gate falsely passed; no minimum geometry rule is added. The local behavior probe used canonical checked-out V4 JavaScript with hash-verified artifact HTML/CSS; the original captures used the exact retained runtime. Exact diagnostics identify the run's two failed inspections and first failed finish as authored build/verification defects, not demonstrated infrastructure failures. The new run costs more and takes longer than the original; these single observations do not support reliability or economic superiority. Detailed evidence is in `fresh-pristine-source-treatment-2026-09-16/` and its version-specific `pristine-source-treatment/` candidate review, beneath `.design/v4-production-readiness-2026-09-04/`.

## Result

The private Pristine Auto Detailing initial build on release `43f4273d79e0a418986b2623b96392cfc9626497` completed normally through the hosted queue. Run `run_37fd0e2e0e29497d7b4bede353a11b3c` produced `version_6d779530fe627e96b46aab6fd0dbc878`: 28 routes, 20m48.164s elapsed, $0.21134310 catalog-estimated model cost, OpenAI `gpt-5.6-luna`, passed hard gate. Two authored build failures were repaired within the normal run. They were a prohibited dynamic property lookup and an invalid top-level return, not evidence for relaxing the source policy. No transport failure is recorded in the collected events. No site was published, inquiry sent, or business contacted.

The prior candidate, Allied, was deferred after its public HTTPS origin failed TLS in the browser; no Allied generation was attempted. That source-readiness failure remains evidence, not a hidden successful sample. Pristine is a diagnostic, not a completed repeatability screen.

All 84 route/viewport captures (1280, 768, and 375 pixels) completed without recorded browser errors, broken visible images, or horizontal overflow. Actual pixel and all-route source review nevertheless rejects unassisted customer delivery. Technical success is not quality acceptance.

## Proven upstream defects and bounded corrections

- The homepage title selector chose the slogan “Austin's Premier Auto Spa” as the business name. The same homepage title also contains “Pristine Auto Detailing,” corroborated by the About and Contact names. A narrowly scoped tie-break now prefers an equally plausible homepage title segment independently selected on at least two other eligible first-party pages. Stronger existing names, ties and insufficient corroboration keep the existing selection. This is name disambiguation, not a general claim that repeated web prose proves truth.
- Broad non-exclusive page-purpose tags promoted fourteen portfolio project titles into service authority and excluded real service routes. Explicit service-route structure now takes precedence over incidental location tags; project/gallery routes remain retained evidence but do not become service names merely because their metadata mentions a service.
- Four specialist pages contain a stale Minnesota contact block. The actual Cedar Park address was missed because the source joins `Building 22A` to its city and `78613` to `USA`, and uses the previously unrecognized street suffix `Terrace`. The address grammar now recognizes these generic forms. Existing scoped-location selection, not a new geographic inference rule, decides among parsed addresses.
- Google research received that Minnesota location even though its address fact was explicitly not public-eligible. Research geography now follows the same eligible-fact authority as public projection. Rejected observations remain auditable but do not become trusted lookup hints. The old ambiguous Google result was appropriate given the conflicting request; no rating has been retroactively inserted.

Focused offline fixtures cover these cases and preserve owner-confirmed authority. Stored inputs, snapshots, workspaces and artifacts remain unchanged. The authoring platform descriptor changes to `site-authoring-platform@sha256:b78e501f2fa155a048d82cd67157f56777de643fa5d2e196f76e138bfdf730b7`; model, prompt, skill, runtime, toolchain and verification rules do not change. Deployment and a corrected fresh build must be recorded separately from local verification.

## Remaining delivery defects

Local verification is complete: full `npm run verify:preflight`, isolated `npm run smoke:dev`, focused ingestion/manager fixtures, TypeScript and whitespace checks passed. Independent review exposed two initial address-regex regressions (ordinary ZIP punctuation and ambiguous joined boundaries); both were fixed and rechecked before release. The final parser keeps ordinary delimiters and a separate conservative joined-building case. Full mocked ingestion proves the actual primary Cedar Park shape becomes public-eligible despite the conflicting specialist page. No broad claim of arbitrary malformed-address recovery follows.

The valid official logo is SVG, excluded by today's raster-only intake. No raw SVG adoption, permissive sanitizer, replacement mark, new dependency or rendering service was added in this correction. Safe SVG-to-raster intake remains a separate format-boundary task.

The author selected only two photos and reused the black Tesla hero across fourteen named vehicle-project routes. This misrepresents project imagery despite otherwise truthful photo alt text. Service bodies are too interchangeable; the long text-only mobile portfolio has excessive empty space. The About page's “Browse the portfolio” button incorrectly goes to Contact, and Auto Detailing was turned into a services hub with a self-link rather than retaining useful package depth. The original source also contains legal placeholders; faithful preservation of inherited defects is not launch-ready legal content.

The responsive shell, open mobile menu, explicit required/optional form guidance and inquiry—not instant-booking—semantics work. No individual review quotes or unsupported Google aggregate were invented. Those strengths do not cancel the defects.

## Next experiment boundary

First verify and release the source-preparation correction, then repeat a fresh private build with unchanged Luna authoring. Check the prepared identity, actual services, geography and Google observation before interpreting the visual result. Preserve this original failure. Do not treat a hand-repaired site as an unassisted pass, add an automatic reviewer, force tool sequences, or raise a subjective quality gate. If the corrected context still yields misleading imagery and shallow route bodies, compare Terra under the same evidence and review criteria rather than assuming more prompt text will solve the judgment issue.

Detailed retained evidence is under `.design/v4-production-readiness-2026-09-04/fresh-pristine-2026-09-16/` and exact-byte captures under the corresponding `candidate-reviews/version_6d779530fe627e96b46aab6fd0dbc878/fresh-pristine/` directory. Reports distinguish inspected pixels from automated coverage and local corrections from hosted deployment.
